interface:
  display_name: "API Contracts"
  short_description: "Define stable Zod API contracts"
  default_prompt: "Use $api-contracts to define or validate endpoint schemas, error responses, and compatibility rules."
